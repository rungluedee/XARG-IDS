# Network Attack Detection Backend -- requirements
# Tested target: Windows 10/11 + Python 3.11 (also works on Linux/macOS).
#
# Install:
#   python -m venv venv
#   venv\Scripts\activate        (Windows)   OR   source venv/bin/activate  (Linux/macOS)
#   pip install -r requirements.txt
#
# Suricata itself is a separate system-level install (not a pip package) --
# see README.md. The pipeline runs fine without it (Tier 1 auto-disables).

# --- data / ML ---
pandas>=2.1,<3.0
numpy>=1.26,<3.0
scikit-learn>=1.3,<2.0
xgboost>=2.0,<3.0
joblib>=1.3

# --- packet capture / flow extraction ---
scapy>=2.5
cicflowmeter>=0.1.6

# --- backend API ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # required by FastAPI for UploadFile/form-data parsing

# --- Tier 3 LLM explanation (optional: falls back to offline templates without an API key) ---
anthropic>=0.34

# --- testing ---
pytest>=8.0
httpx>=0.27               # required by fastapi.testclient.TestClient
